<UserControl x:Name="IdentifiedFeatureUserControl"
    x:Class="Esri.ArcGISRuntime.ExampleApps.DataCollection.UWP.Views.IdentifiedFeaturePopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Esri.ArcGISRuntime.ExampleApps.DataCollection.Shared.Converters"
    xmlns:local="using:Esri.ArcGISRuntime.ExampleApps.DataCollection.UWP.Views"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:DebugConverter x:Key="DebugConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Pivot>
            <PivotItem Header="Details">
                <ScrollViewer VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <!--List of attributes from identified feature in View Mode-->
                        <ItemsControl ItemsSource="{x:Bind IdentifiedFeatureViewModel.PopupManager.DisplayedFields, Mode=OneWay}" Grid.Row="0"
                                      Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter},ConverterParameter=Inverse}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Field.Label, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" Margin="10, 5, 10, 0" />
                                        <TextBlock Text="{Binding FormattedValue, Mode=OneWay}" TextWrapping="Wrap" Style="{StaticResource BaseTextBlockStyle}" Margin="12, 5, 10, 5"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--List of relationships where identified feature is destination-->
                        <ItemsControl ItemsSource="{x:Bind IdentifiedFeatureViewModel.DestinationRelationships, Mode=OneWay}" HorizontalContentAlignment="Stretch" Grid.Row="1"
                                      Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter},ConverterParameter=Inverse}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Visibility="{Binding PopupManager, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="{Binding PopupManager.Title}" Grid.Row="0" Style="{StaticResource SubtitleTextBlockStyle}" Margin="10, 10, 10, 5"/>
                                        <StackPanel Grid.Column="0" Grid.Row="1">
                                            <TextBlock Text="{Binding PopupManager.DisplayedFields[0].Field.Label, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}"  Margin=" 10,5,0,0"/>
                                            <TextBlock Text="{Binding PopupManager.DisplayedFields[0].FormattedValue, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Margin="12,5,0,5"/>
                                            <TextBlock Text="{Binding PopupManager.DisplayedFields[1].Field.Label, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" Margin=" 10,5,0,0"/>
                                            <TextBlock Text="{Binding PopupManager.DisplayedFields[1].FormattedValue, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Margin="12,5,0,5"/>
                                        </StackPanel>
                                        <Button Content="&#xE76C;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource TextBlockButtonStyle}" Width="30" Height="30"
                                                Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,15,0" 
                                                Command="{Binding ElementName=IdentifiedFeatureUserControl, Mode=OneWay, Path=DataContext.IdentifiedFeatureViewModel.SetSelectedDestinationRelationshipCommand}" CommandParameter="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--List of relationships where identified feature is origin-->
                        <ItemsControl ItemsSource="{x:Bind IdentifiedFeatureViewModel.OriginRelationships, Mode=OneWay}" HorizontalContentAlignment="Stretch" Grid.Row="2"
                                      Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter},ConverterParameter=Inverse}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="40"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding RelatedTable.TableName, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" Padding="10" Grid.Column="0"/>
                                        </Grid>
                                        <ItemsControl ItemsSource="{Binding OriginRelatedRecords,Mode=OneWay}"  HorizontalContentAlignment="Stretch">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="40"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="5"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>
                                                        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="1" x:Name="OriginRecordGrid">
                                                            <TextBlock Text="{Binding DisplayedFields[0].Field.Label, Mode=OneWay}"  Style="{StaticResource BodyTextBlockStyle}"  Margin=" 10,5,0,0"/>
                                                            <TextBlock Text="{Binding DisplayedFields[0].FormattedValue, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Margin="12,5,0,5"/>
                                                            <TextBlock Text="{Binding DisplayedFields[1].Field.Label, Mode=OneWay}"  Style="{StaticResource BodyTextBlockStyle}"  Margin=" 10,5,0,0"/>
                                                            <TextBlock Text="{Binding DisplayedFields[1].FormattedValue, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Margin="12,5,0,5"/>
                                                            <TextBlock Text="{Binding DisplayedFields[2].Field.Label, Mode=OneWay}"  Style="{StaticResource BodyTextBlockStyle}"  Margin=" 10,5,0,0"/>
                                                            <TextBlock Text="{Binding DisplayedFields[2].FormattedValue, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Margin="12,5,0,5"/>
                                                        </StackPanel>
                                                        <Button Content="&#xE76C;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource TextBlockButtonStyle}" Width="30" Height="30" VerticalAlignment="Center"
                                                            Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,15,0" 
                                                            Command="{Binding ElementName=IdentifiedFeatureUserControl, Mode=OneWay, Path=DataContext.IdentifiedFeatureViewModel.SetSelectedOriginRelationshipCommand}" 
                                                            CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!--List of attributes from identified feature in View Mode-->
                        <local:FeatureEditorView DataContext="{x:Bind IdentifiedFeatureViewModel}"
                                                 Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay,Converter={StaticResource NullToVisibilityConverter}}"/>

                    </Grid>
                </ScrollViewer>
            </PivotItem>
            <PivotItem Header="Attachments">
                <!-- Attachments view -->
                <local:AttachmentsView DataContext="{x:Bind IdentifiedFeatureViewModel, Mode=OneWay}"/>
            </PivotItem>
        </Pivot>

        <!-- Buttons to edit and delete record -->
        <CommandBar Grid.Row="1" Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter},ConverterParameter=Inverse}">
            <AppBarButton Icon="Edit" Command="{x:Bind IdentifiedFeatureViewModel.EditFeatureCommand, Mode=OneWay}"/>
            <AppBarButton Icon="Delete" Command="{Binding ElementName=IdentifiedFeatureUserControl, Mode=OneWay, Path=DataContext.DeleteFeatureCommand}" CommandParameter="{x:Bind IdentifiedFeatureViewModel}"/>
        </CommandBar>

        <!-- Buttons to save and discard changes to record -->
        <CommandBar Grid.Row="1" Visibility="{x:Bind IdentifiedFeatureViewModel.EditViewModel, Mode=OneWay,Converter={StaticResource NullToVisibilityConverter}}">
            <AppBarButton Icon="Save" Command="{x:Bind IdentifiedFeatureViewModel.SaveEditsCommand, Mode=OneWay}"/>
            <AppBarButton Icon="Cancel" Command="{Binding ElementName=IdentifiedFeatureUserControl, Mode=OneWay, Path=DataContext.CancelEditsCommand}" CommandParameter="{x:Bind IdentifiedFeatureViewModel}"/>
        </CommandBar>
    </Grid>
</UserControl>
