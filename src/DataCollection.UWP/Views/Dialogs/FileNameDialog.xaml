<ContentDialog
    x:Class="Esri.ArcGISRuntime.OpenSourceApps.DataCollection.UWP.Views.FileNameDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:converters="using:Esri.ArcGISRuntime.OpenSourceApps.DataCollection.Shared.Converters"
    Title="{x:Bind Converter={StaticResource LC}, ConverterParameter=AttachmentNameDialog_Title}"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    Background="{ThemeResource chrome-background}"
    Foreground="{ThemeResource chrome-foreground}"
    PrimaryButtonText="{x:Bind Converter={StaticResource LC}, ConverterParameter=AttachmentNameDialog_PrimaryButtonText}"
    mc:Ignorable="d">

<!-- TODO - set requested theme globally with resource-->
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBox Style="{StaticResource CardEditorTextBox}"
                x:Name="FileNameTextBox"
                Text="{x:Bind Converter={StaticResource LC}, ConverterParameter=AttachmentNameDialog_DefaultAttachmentName}"
                TextChanged="FileNameTextBox_TextChanged" />
            <TextBlock x:Name="ValidationTextBlock" Style="{StaticResource CardValidationErrorText}" />
        </StackPanel>
    </Grid>
</ContentDialog>
